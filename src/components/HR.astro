---
export interface Props {
  class?: string;
}
const {class:classes = "my-6"} = Astro.props;
---

<div class={`${classes} w-[90%] block mx-auto h-1 bg-gradient-to-r from-blue-6 via-30% via-purple-6 to-pink-5 rounded-2xl`}>
</div>
